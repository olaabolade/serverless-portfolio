name: Build Docker Image
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Docker image
        run: docker build -t portfolio .
      - name: Test image
        run: docker run --rm portfolio echo "Build successful"
